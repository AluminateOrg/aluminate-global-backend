package com.aluminate.aluminate_global_backend.repository;

import com.aluminate.aluminate_global_backend.model.OrgPayout;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrgPayoutRepository extends JpaRepository<OrgPayout, Long> {
}
