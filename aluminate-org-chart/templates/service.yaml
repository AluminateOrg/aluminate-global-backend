apiVersion: v1
kind: Service
metadata:
  name: {{ include "aluminate-org-chart.fullname" . }}
  labels:
    {{- include "aluminate-org-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aluminate-org-chart.selectorLabels" . | nindent 4 }}
